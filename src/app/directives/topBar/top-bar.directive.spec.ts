import { TopBarDirective } from './top-bar.directive';

describe('TopBarDirective', () => {
  it('should create an instance', () => {
    const directive = new TopBarDirective();
    expect(directive).toBeTruthy();
  });
});
